---
- name: Speedtest ausführen und Ergebnis speichern
  hosts: speedtest
  become: yes
  tasks:
    - name: Speedtest ausführen
      command: speedtest --format=human-readable > /tmp/result.txt
      async: 60
      poll: 0
      register: speedtest_job

